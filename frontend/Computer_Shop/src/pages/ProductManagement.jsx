import React from 'react';
// We'll reuse the existing AdminDashboard component
import AdminDashboard from './AdminDashboard';

const ProductManagement = () => {
  return <AdminDashboard />;
};

export default ProductManagement; 